name: Keep Backend Awake
on:
  schedule:
    - cron: '*/14 * * * *' # Se ejecuta cada 14 minutos
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render API
        run: curl -s https://tu-backend.onrender.com/api/schema/ > /dev/null